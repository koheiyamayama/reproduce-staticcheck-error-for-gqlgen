type Query {
  health: String!
}

" @goField (gqlgen builtin) "
directive @goField(
    forceResolver: Boolean
    name: String
) on INPUT_FIELD_DEFINITION | FIELD_DEFINITION

enum Device {
  IPHONE
}
